import { Suspense } from "react";
import { AthleteHero } from "@/components/sport/AthleteHero";
import { AthleteStats } from "@/components/sport/AthleteStats";
import HighlightsSection from "@/components/sport/HighlightsSection";
import RecruiterForm from "@/components/sport/RecruiterForm";

export default function HomePage() {
  return (
    <Suspense fallback={<div className="min-h-screen bg-black flex items-center justify-center text-white">Loading Torey&apos;s Profile...</div>}>
      <main className="min-h-screen bg-black">
        <AthleteHero />
        <AthleteStats />
        <HighlightsSection />
        <RecruiterForm />
      </main>
    </Suspense>
  );
}
